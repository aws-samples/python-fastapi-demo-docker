apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: fargate-quickstart
  region: us-west-2

iam:
  withOIDC: true

addons:
- name: adot
  # all below properties are optional
  version: latest
  tags:
    team: eks
  
